import "./style.css";

const SearchBar = () => {
  return html;
};

const html = (
  <div className="header__search-bar">
    <input type="text" className="header__search-bar--input" />
    <button className="header__search-bar--btn">
      <i className="fa-solid fa-magnifying-glass"></i>
    </button>
  </div>
);

export default SearchBar;
